# Flick 剩余工作交接待办清单

## 1. 当前状态

- 项目目录：`/Users/tape/Documents/Flick`
- 当前分支：`OpenRoter_build`
- 最近完成的任务：T32「真实构建与核心回归」
- 最近提交：`54a1124 test: complete feature regression`
- 正式任务进度：T01–T32 已完成，T33 未完成
- 交接时工作区应保持干净；接手后请先运行 `git status --short` 核实

> 安全要求：项目测试使用的 OpenRouter API Key 已由用户配置在应用中。不得读取、输出、写入文档、写入源码或提交该密钥。

## 2. 必做待办

### TODO 1：完成 T33，更新 README 与使用说明

- 状态：`[x]` — 已完成（见 commit e922b23）
- 目标：让 README 准确反映当前已经实现的功能。
- 建议修改文件：
  - `README.md`
  - `任务清单.md`

README 至少需要补充：

1. 支持的常用 Markdown：
   - 标题
   - 普通段落
   - 粗体、斜体、删除线
   - 行内代码
   - 链接
   - 有序列表、无序列表
   - 引用
   - 分隔线
   - 围栏代码块
2. 每个代码块具有独立复制按钮。
3. 流式回复期间持续渲染 Markdown；未闭合标记暂时显示为普通文本。
4. 用户主动滚动阅读旧内容时，自动滚动必须暂停；用户回到底部后恢复跟随。
5. 提示词列表保持固定尺寸，只有 AI 回复页可以拖拽缩放。
6. 回复窗口最小尺寸为 `380 × 360 pt`，宽度和高度的理论最大值分别不超过最小值的五倍，同时受屏幕可用区域限制。
7. 用户调整后的回复窗口尺寸会保存；下次进入回复页时恢复。
8. 回复页右上角提供图钉：
   - 固定后图标点亮。
   - 固定窗口保持在普通应用窗口上方。
   - 取消固定后不会立即关闭，等待下一次窗口外部点击。
9. 支持多个独立结果窗口，新窗口自动错开；每个窗口可独立回复、移动、缩放和固定。
10. 浮窗不再显示余额。
11. OpenRouter 余额位于“设置 → 通用 → API 配置”：
    - 打开设置时自动刷新。
    - 显示美元余额并保留两位小数。
    - 显示相对刷新时间。
    - 显示连接、加载、失败或旧数据状态。
    - 不提供手动刷新按钮。
    - API Key 为空时显示配置提醒。
    - 非 OpenRouter API 地址时隐藏整个账户区域。
12. 最低支持版本为 macOS 26。

需要删除或改写的过时内容：

- README 中要求用户“暂时禁用任何 VPN 或防火墙”的排障建议过于宽泛，不应作为默认操作。请改为先检查 API 地址、网络连通性、代理配置和应用日志。
- 不得宣称尚未实现、实际不存在或没有验证过的功能。

验收标准：

- README 中的操作步骤可以对应到当前应用界面。
- README 不再包含与现有实现冲突的描述。
- `任务清单.md` 中 T33 状态改为 `[x]`，并追加实际修改内容和验证记录。
- 文档检查通过：`git diff --check`。
- Xcode Debug 构建成功。
- T33 单独创建一个 commit，建议提交信息：

```text
docs: document floating panel enhancements
```

### TODO 2：同步需求与技术方案的实现状态

- 状态：`[x]` — 已完成（见 commit e922b23）
- 目标：清除已经过时的“尚未进入开发”描述。
- 建议修改文件：
  - `需求说明.md`
  - `技术方案.md`

需要处理：

1. `需求说明.md` 顶部仍写有“实现状态：尚未进入开发”。
2. `技术方案.md` 顶部仍写有“实现状态：尚未进入开发”。
3. `技术方案.md` 仍称 `swift-markdown` 尚未实际接入，但项目已经锁定并使用 `swift-markdown 0.8.0`。
4. 只更新实现状态和已经被事实推翻的描述，不重写已经确认的产品范围。

验收标准：

- 两份文档不再把已完成的项目描述为未开发。
- `swift-markdown` 的描述与工程实际依赖一致。
- 不把尚未验证的内容写成已完成。
- 建议与 TODO 1 合并在 T33 的同一个文档提交中。

## 3. 已知问题与建议后续任务

### TODO 3：调查 OpenRouter 请求偶尔长时间停留在"处理中"

- 状态：`[x]` — 已完成（见 T34）
- 完成内容：
  - 代码分析确认了两个根因：`streamChat()` 未设 `timeoutInterval` 且 `connectionProxyDictionary = [:]` 清空了系统代理。
  - `AIService.swift` 修复：添加 `request.timeoutInterval = 30`；移除 `connectionProxyDictionary = [:]`，改用系统默认代理；`URLSession` 改为可注入实例属性。
  - 新增 `AIServiceNetworkTests`，使用 `MockStreamURLProtocol` 覆盖首字节超时、HTTP 401、空流、正常流和用户取消共 5 项测试。
  - 全部 23 项测试通过（原有 18 项 + 新增 5 项）。
  - commit: `修复流式请求超时和代理配置（TODO 3 对应的独立 commit 哈希）`
- 遗留说明：
  - 真实的 OpenRouter 服务端响应速度仍需用户在配置了 Key 的情况下手工验收确认。
  - 流中数据停止后依赖 URLSession 的 `timeoutIntervalForRequest`（默认 60 秒）触发超时；如需更短的流空闲超时，需增加 per-line 超时机制，这不在本次修改范围内。

## 4. 接手时需要注意

1. 严格按一个任务一个 commit 推进。
2. 每项任务必须实际验收后才能标记完成。
3. 不要重复实现 T01–T32 已完成的功能。
4. 不要修改与当前 TODO 无关的代码。
5. 不要提交 DerivedData、测试结果包、临时文件或任何凭据。
6. 不要把静态代码检查描述成真实运行验收。
7. 如果发现工作区已有用户改动，必须保留，不得覆盖或重置。
8. 禁止使用 `git reset --hard` 或其他破坏性清理命令。

## 5. 当前已有验证证据

- 完整 Xcode 测试：18 项通过。
- 多窗口会话管理测试曾连续运行 5 次并全部通过。
- 真实 GUI 已验证：
  - 用户选中文字并按 `⌘E` 能打开浮窗。
  - OpenRouter 能返回真实回复。
  - Markdown 代码块显示独立复制按钮。
  - 图钉点亮并把窗口提升到浮动层。
  - 回复窗口可以从 `380 × 360 pt` 拖拽到 `622 × 522 pt`。
  - 新结果窗口恢复保存的 `622 × 522 pt` 尺寸。
  - 两个结果窗口可以同时存在并位于不同坐标。
  - 设置页显示 OpenRouter 连接状态、两位小数余额和相对刷新时间。
- T32 验收和已知限制已记录在 `任务清单.md`。

## 6. 发给 VS Code 的提示词

```text
请接手 `/Users/tape/Documents/Flick` 项目的剩余工作。

开始前请先完整阅读：
1. `交接待办清单.md`
2. `任务清单.md`
3. `需求说明.md`
4. `技术方案.md`
5. `README.md`

当前分支应为 `OpenRoter_build`，最近完成提交为：
`54a1124 test: complete feature regression`

工作规则：
- 先运行只读检查，确认当前分支、最近提交和工作区状态。
- 不要重复实现 T01–T32。
- 先完成 T33 文档更新和验收，再考虑任何后续优化。
- 每次只处理一个可独立验收的任务。
- 每项任务必须通过验收后再创建一个独立 commit。
- 不得读取、打印、写入或提交 OpenRouter API Key。
- 不得覆盖用户已有改动，不得使用 `git reset --hard`。
- 没有实际运行的检查，不得描述为“已测试”或“已完成”。

第一步请完成：
1. 更新 README，使其准确说明 Markdown、用户滚动优先、回复窗口缩放与尺寸记忆、图钉固定、多结果窗口，以及设置页 OpenRouter 余额和连接状态。
2. 改写 README 中“暂时禁用 VPN 或防火墙”的宽泛建议。
3. 更新 `需求说明.md` 和 `技术方案.md` 中已经过时的“尚未进入开发”状态。
4. 把 `技术方案.md` 中关于 `swift-markdown` 尚未接入的过时描述改为与工程实际依赖一致。
5. 完成文档检查和 Xcode Debug 构建。
6. 验收通过后，把 `任务清单.md` 的 T33 标记为完成并写入真实验收记录。
7. 创建独立 commit，建议提交信息：
   `docs: document floating panel enhancements`

注意：
OpenRouter 偶尔长时间停留在“处理中”是独立的后续调查项，不要在 T33 中顺手修改网络代码。先完成并提交 T33，然后向我报告：
- 修改了哪些文件
- 实际运行了哪些验证
- 验证结果
- commit 哈希
- 仍有哪些未处理 TODO
```
